# AUREA AI — GitHub / Railway MVP

## Quick start (local / dev)
1. Copy `backend/config.example.py` → `backend/config.py` and set RPC_URL and POLYGON_USDT_ADDRESS.
2. Create virtualenv, install:
   ```bash
   python -m venv venv
   source venv/bin/activate
   pip install -r backend/requirements.txt
   
